---
/**
 * T17 - DM Sans Warm
 * Friendly Modern Sans-Serif
 * Warm Amber palette
 */
---

<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>T17: DM Sans Warm | Typografie-Variante</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --color-amber: #d4a24e;
      --color-amber-light: #e0b562;
      --color-navy: #0f1318;
      --color-navy-light: #181e26;
      --color-cream: #faf6ed;
      --font-sans: 'DM Sans', -apple-system, sans-serif;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: var(--font-sans);
      color: var(--color-cream);
      background: var(--color-navy);
      line-height: 1.6;
    }

    .variant-tag {
      position: fixed;
      top: 20px;
      right: 20px;
      background: var(--color-amber);
      color: var(--color-navy);
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 0.75rem;
      font-weight: 700;
      z-index: 100;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 24px;
    }

    .header {
      padding: 30px 0;
      border-bottom: 1px solid rgba(212, 162, 78, 0.12);
    }

    .header-inner {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
    }

    .logo-icon {
      width: 44px;
      height: 44px;
      background: var(--color-amber);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--color-navy);
      font-weight: 700;
      font-size: 0.95rem;
    }

    .logo-text {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--color-cream);
    }

    .nav {
      display: flex;
      gap: 32px;
    }

    .nav a {
      color: rgba(250, 246, 237, 0.55);
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 500;
      transition: color 0.2s;
    }

    .nav a:hover, .nav a.active {
      color: var(--color-cream);
    }

    .hero {
      padding: 110px 0 100px;
      text-align: center;
    }

    .hero-label {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(212, 162, 78, 0.12);
      border: 1px solid rgba(212, 162, 78, 0.2);
      color: var(--color-amber);
      padding: 10px 22px;
      border-radius: 6px;
      font-size: 0.8rem;
      font-weight: 600;
      margin-bottom: 32px;
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 700;
      line-height: 1.12;
      color: var(--color-cream);
      margin-bottom: 22px;
      letter-spacing: -0.01em;
    }

    .hero h1 span {
      color: var(--color-amber);
    }

    .hero .lead {
      font-size: 1.125rem;
      color: rgba(250, 246, 237, 0.7);
      line-height: 1.75;
      max-width: 550px;
      margin: 0 auto 40px;
      font-weight: 400;
    }

    .cta-buttons {
      display: flex;
      gap: 16px;
      justify-content: center;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 16px 32px;
      font-weight: 600;
      font-size: 0.95rem;
      text-decoration: none;
      border-radius: 8px;
      transition: all 0.2s;
    }

    .btn-primary {
      background: var(--color-amber);
      color: var(--color-navy);
    }

    .btn-primary:hover {
      background: var(--color-amber-light);
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(212, 162, 78, 0.3);
    }

    .btn-secondary {
      background: transparent;
      color: var(--color-cream);
      border: 1px solid rgba(250, 246, 237, 0.2);
    }

    .btn-secondary:hover {
      border-color: var(--color-cream);
    }

    .features {
      padding: 100px 0;
      background: var(--color-navy-light);
    }

    .features-header {
      text-align: center;
      margin-bottom: 50px;
    }

    .features-header h2 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--color-cream);
      margin-bottom: 10px;
    }

    .features-header p {
      color: rgba(250, 246, 237, 0.6);
      font-size: 1rem;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 28px;
    }

    .feature-card {
      background: var(--color-navy);
      border: 1px solid rgba(212, 162, 78, 0.1);
      border-radius: 12px;
      padding: 36px 28px;
      text-align: center;
      transition: all 0.3s;
    }

    .feature-card:hover {
      border-color: rgba(212, 162, 78, 0.3);
      transform: translateY(-4px);
    }

    .feature-icon {
      width: 52px;
      height: 52px;
      background: rgba(212, 162, 78, 0.1);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 20px;
    }

    .feature-icon svg {
      width: 26px;
      height: 26px;
      stroke: var(--color-amber);
    }

    .feature-card h3 {
      font-size: 1.125rem;
      font-weight: 600;
      color: var(--color-cream);
      margin-bottom: 10px;
    }

    .feature-card p {
      color: rgba(250, 246, 237, 0.6);
      font-size: 0.9rem;
      line-height: 1.6;
    }

    .typo-info {
      padding: 80px 0;
      text-align: center;
      border-top: 1px solid rgba(212, 162, 78, 0.1);
    }

    .typo-info h2 {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--color-amber);
      margin-bottom: 40px;
    }

    .font-sample {
      display: flex;
      justify-content: center;
      gap: 70px;
    }

    .font-sample div { text-align: left; }

    .font-sample h4 {
      font-size: 0.75rem;
      color: rgba(250, 246, 237, 0.5);
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 8px;
    }

    .sample-headline {
      font-size: 2rem;
      font-weight: 700;
      color: var(--color-cream);
    }

    .sample-body {
      font-size: 1.25rem;
      font-weight: 400;
      color: var(--color-cream);
    }

    .color-palette {
      display: flex;
      justify-content: center;
      gap: 22px;
      margin-top: 48px;
    }

    .palette-item { text-align: center; }

    .palette-swatch {
      width: 56px;
      height: 56px;
      border-radius: 10px;
      margin-bottom: 8px;
      border: 2px solid rgba(250, 246, 237, 0.1);
    }

    .palette-name {
      font-size: 0.7rem;
      color: rgba(250, 246, 237, 0.5);
      margin-bottom: 2px;
    }

    .palette-hex {
      font-size: 0.65rem;
      color: var(--color-amber);
      font-family: monospace;
    }

    @media (max-width: 768px) {
      .nav { display: none; }
      .hero h1 { font-size: 2.5rem; }
      .cta-buttons { flex-direction: column; }
      .features-grid { grid-template-columns: 1fr; }
      .font-sample { flex-direction: column; gap: 32px; align-items: center; }
    }
  </style>
</head>
<body>
  <div class="variant-tag">T17: DM Sans Warm</div>

  <header class="header">
    <div class="container">
      <div class="header-inner">
        <a href="/" class="logo">
          <div class="logo-icon">SH</div>
          <span class="logo-text">Susanne Hoppe</span>
        </a>
        <nav class="nav">
          <a href="#" class="active">Home</a>
          <a href="#">Über mich</a>
          <a href="#">Leistungen</a>
          <a href="#">Kontakt</a>
        </nav>
      </div>
    </div>
  </header>

  <section class="hero">
    <div class="container">
      <div class="hero-label">Mobile Kinderosteopathie Leipzig</div>
      <h1>Sanfte <span>Osteopathie</span><br>für Ihr Kind</h1>
      <p class="lead">
        Begleitende osteopathische Behandlung für Säuglinge, Kinder und ihre Familien –
        einfühlsam und bei Ihnen zuhause.
      </p>
      <div class="cta-buttons">
        <a href="#" class="btn btn-primary">Termin anfragen</a>
        <a href="#" class="btn btn-secondary">Mehr erfahren</a>
      </div>
    </div>
  </section>

  <section class="features">
    <div class="container">
      <div class="features-header">
        <h2>Warum zu mir?</h2>
        <p>Drei gute Gründe für Ihre Entscheidung</p>
      </div>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
              <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            </svg>
          </div>
          <h3>Bei Ihnen zuhause</h3>
          <p>Entspannte Behandlung in vertrauter Umgebung für Ihr Kind.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">
            <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
              <path d="M12 2L2 7l10 5 10-5-10-5z M2 17l10 5 10-5"/>
            </svg>
          </div>
          <h3>Klinische Erfahrung</h3>
          <p>Tätig auf einer Neugeborenenstation mit medizinischer Expertise.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">
            <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
            </svg>
          </div>
          <h3>Für die Familie</h3>
          <p>Ganzheitliche Begleitung für Kinder und ihre Familien.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="typo-info">
    <div class="container">
      <h2>Typografie & Farben dieser Variante</h2>
      <div class="font-sample">
        <div>
          <h4>Headlines</h4>
          <p class="sample-headline">DM Sans 700</p>
        </div>
        <div>
          <h4>Body</h4>
          <p class="sample-body">DM Sans 400</p>
        </div>
      </div>
      <div class="color-palette">
        <div class="palette-item">
          <div class="palette-swatch" style="background: #0f1318;"></div>
          <div class="palette-name">Warm Navy</div>
          <div class="palette-hex">#0f1318</div>
        </div>
        <div class="palette-item">
          <div class="palette-swatch" style="background: #d4a24e;"></div>
          <div class="palette-name">Amber</div>
          <div class="palette-hex">#d4a24e</div>
        </div>
        <div class="palette-item">
          <div class="palette-swatch" style="background: #faf6ed;"></div>
          <div class="palette-name">Warm Cream</div>
          <div class="palette-hex">#faf6ed</div>
        </div>
      </div>
    </div>
  </section>
</body>
</html>
